/**
 * Type declarations for tsx/esm
 * tsx/esm is imported as a side-effect to register TypeScript loader hooks
 */
declare module "tsx/esm" {
  // This module is imported for its side effects (registering TypeScript loader)
  // No exports are needed
}
